{
  "name": "wabiseminar-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd backend && npm start",
    "build": "cd backend && npm install && cd ../frontend && npm install && VITE_API_URL=/api npm run build"
  },
  "dependencies": {
    "socket.io-client": "^4.8.3"
  }
}
